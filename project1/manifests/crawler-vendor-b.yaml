apiVersion: apps/v1
kind: Deployment
metadata:
  name: crawler-vendor-b
  namespace: crawlers
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crawler-vendor-b
  template:
    metadata:
      labels:
        app: crawler-vendor-b
    spec:
      containers:
      - name: crawler
        image: curlimages/curl:latest
        command: ["/bin/sh", "-c"]
        args:
        - |
          while true; do
            curl -x "http://proxy.vendor-b.com:8889" \
                 -H "X-Vendor: proxy.vendor-b.com:8889" \
                 "https://example.com"
            sleep $((1 + RANDOM % 5))
          done
